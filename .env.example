# ===========================================
# ZOIRO BROAST HUB - ENV]]I0ROx0N00sMaaNT VARIABLES
# ===========================================
# Copy this file to .env and fill in your values
# NEVER commit .env to vegrsion control!
# 
# For Vercel deployment, add these in your Vercel project settings:
# Settings > Environment Variables

# ----- SUPABASE -----
# Get these from your Supabase project settings > API
# Use VITE_ prefix (primary) - code supports both prefixes
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ----- REDIS (UPSTASH) -----
# Get these from your Upstash console
# Used for rate limiting and caching
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token-here

# ----- SECURITY -----
# Generate with: openssl rand -base64 64
# Used for JWT token signing
JWT_SECRET=your-super-secret-jwt-key-here-minimum-32-characters

# ----- BREVO (EMAIL SERVICE) -----
# Get from your Brevo account > SMTP & API
# Used for sending transactional emails
BREVO_API_KEY=your-brevo-api-key-here
SENDER_EMAIL=info@zoirobroast.me

# ----- APP CONFIG -----
# Production URL: https://zoirobroast.me
# Development URL: http://localhost:3000
NEXT_PUBLIC_APP_URL=https://zoirobroast.me

# ===========================================
# VERCEL DEPLOYMENT CHECKLIST:
# ===========================================
# 1. Add VITE_SUPABASE_URL
# 2. Add VITE_SUPABASE_ANON_KEY  
# 3. Add JWT_SECRET
# 4. Add BREVO_API_KEY
# 5. Add SENDER_EMAIL
# 6. Add UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN
# 7. Add NEXT_PUBLIC_APP_URL = https://zoirobroast.me
# ===========================================
